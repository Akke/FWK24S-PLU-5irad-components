import RestartIcon from './RestartIcon';

const meta = {
  component: RestartIcon,
};

export default meta;

export const Default = {
  args: {
    size: 24,
    color: "currentColor",
    strokeWidth: 2
  }
};