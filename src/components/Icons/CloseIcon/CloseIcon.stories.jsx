import CloseIcon from './CloseIcon';

const meta = {
  component: CloseIcon,
};

export default meta;

export const Default = {
  args: {
    size: 24,
    color: "currentColor",
    strokeWidth: 2
  }
};